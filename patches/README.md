# Runtime Patches

No active runtime patches are currently required.

This directory is intentionally kept so targeted temporary patches can be
added during image builds when upstream regressions block development or
restores. Any new patch should include:

- the upstream issue reference
- the exact symptom it mitigates
- a clear removal condition
