# AGENTS.md — image_enhancements

## Scope

- Extend `image.mixin` with file size and resolution helpers.
- Keep logic generic and reusable across addons.

## Notes

- Pillow dependency is declared in `pyproject.toml`.
