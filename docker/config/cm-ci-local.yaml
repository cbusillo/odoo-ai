# CM CI local compose overlay.
# Applied after docker/config/base.yaml and before variant overlays.

services:
  web:
    env_file:
      - docker/config/cm-ci-local.env
  script-runner:
    env_file:
      - docker/config/cm-ci-local.env
  database:
    env_file:
      - docker/config/cm-ci-local.env
