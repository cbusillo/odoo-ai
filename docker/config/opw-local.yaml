# Variant-specific overrides for the OPW local stack.

services:
  web:
    command: /odoo/odoo-bin --dev=all
    ports:
      - "8069:8069"
      - "8072:8072"
  database:
    ports:
      - "15432:5432"
