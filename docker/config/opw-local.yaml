# OPW local compose overlay.
# Applied after docker/config/base.yaml and before variant overlays.

services:
  web:
    env_file:
      - docker/config/opw-local.env
  script-runner:
    env_file:
      - docker/config/opw-local.env
  database:
    env_file:
      - docker/config/opw-local.env
