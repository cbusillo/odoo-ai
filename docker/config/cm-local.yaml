# CM local compose overlay.
# Applied after docker/config/base.yaml and before variant overlays.

services:
  web:
    env_file:
      - docker/config/cm-local.env
  script-runner:
    env_file:
      - docker/config/cm-local.env
  database:
    env_file:
      - docker/config/cm-local.env
