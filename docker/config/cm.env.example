# CM project-level defaults.
# Copy to docker/config/cm.env and adjust per environment.

ODOO_PROJECT_NAME=cm
ODOO_STATE_ROOT=/opt/odoo-ai/data/cm

# Module update list (optional)
ODOO_UPDATE=AUTO
# Explicit list of addons to install/update when ODOO_UPDATE=AUTO
ODOO_AUTO_MODULES=cm_custom
# Colon/comma delimited addon directories scanned when ODOO_UPDATE=AUTO
LOCAL_ADDONS_DIRS=/volumes/addons/cm:/volumes/enterprise
